# Binary search

Searching for element x in a N-element array of type T. The worst case scenario is O(log n).

## build

STD 
```cmake
cmake ../cmake/
make 
```

## Launch

`./*.o -s 0 -n 100 -f 1600`